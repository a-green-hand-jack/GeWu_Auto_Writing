---
name: runtime-smoke
description: Temporary Phase-0 probe for checking runtime context and the isolated tool environment; not a product skill.
---

# Runtime Smoke

TODO: replace during implementation.

This is the only retained, renamed fragment of the HeWo smoke shape. It is temporary infrastructure and must not be expanded into product functionality.

When explicitly asked for an infrastructure check, run `paperwriter-infra-check --check`, verify the exact output `PAPERWRITER_INFRA_OK`, and return that exact text. Do not infer or claim any product identity, writing capability, workflow, or acceptance behavior.
